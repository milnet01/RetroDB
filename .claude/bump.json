{
  "$comment": "Recipe consumed by ~/.claude/skills/bump/SKILL.md. RetroDB's source of truth is config.py's APP_VERSION; APP_LAST_UPDATE bumps to today on every release. Changelog body lives in data/changelog.yaml as a YAML list with HTML <li> bodies — a todo because the body content can't be auto-generated.",
  "version_source": "config.py",
  "version_pattern": "APP_VERSION\\s*=\\s*\"([0-9]+\\.[0-9]+\\.[0-9]+)\"",
  "files": [
    {
      "path": "config.py",
      "pattern": "APP_VERSION = \"{OLD}\"",
      "replace": "APP_VERSION = \"{NEW}\""
    }
  ],
  "post_check": "grep -E 'APP_VERSION = \"[0-9]+\\.[0-9]+\\.[0-9]+\"' config.py",
  "todos": [
    "Update APP_LAST_UPDATE in config.py to {TODAY} (the recipe's pattern-edit is too broad to match the date safely; do it by hand).",
    "Prepend a new entry to data/changelog.yaml. Format: `- version: {NEW}` / `  date: {TODAY}` / `  tags:` / `    - type: <feat|fix|breaking>` / `      label: <label>` / `  body: |` / `    <ul><li>…</li></ul>`. Mirror the body of the most recent entry as a style reference.",
    "Rebuild minified assets if any CSS/JS changed: `python3 build_css.py && python3 build_js.py`.",
    "Smoke-test: launch the app and verify the new APP_VERSION shows in the footer / about page."
  ]
}
