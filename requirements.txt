# =============================================================================
# RETRODB - Python Dependencies
# =============================================================================
# Install with: pip install -r requirements.txt
# =============================================================================

# Web Framework
Flask>=3.1.3,<4.0

# Production WSGI server (replaces Flask dev server)
waitress>=3.0.2,<4.0

# HTTP Requests (for API scraping)
requests>=2.33.1,<3.0

# YAML parsing (for changelog data)
PyYAML>=6.0.3,<7.0

# Image processing (scraper images, avatars, screenshots, image standardization)
Pillow>=12.2.0,<13.0

# PSN trophy integration
PSNAWP>=3.0.3,<4.0

# CLZ Games PDF import
pdfplumber>=0.11.9,<1.0

# AI image upscaling (Real-ESRGAN)
onnxruntime>=1.25.1,<2.0
numpy>=2.4.4,<3.0

# API rate limiting for Flask routes
Flask-Limiter>=4.1.1,<5.0

# Circuit breaker for scraper API resilience
circuitbreaker>=2.1.3,<3.0

# Hardened XML parser (XXE / billion-laughs defense for ES-DE gamelist.xml + RPCS3 trophy files)
defusedxml>=0.7.1,<1.0

# Optional: Background removal for controller images
# rembg>=2.0.0
